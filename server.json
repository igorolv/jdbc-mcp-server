{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.igorolv/jdbc-mcp-server",
  "title": "JDBC MCP Server",
  "description": "Read-only SQL, plans, schema for AI agents: PostgreSQL, Oracle, SQL Server, Firebird, SQLite, JDBC",
  "version": "0.0.0",
  "websiteUrl": "https://github.com/igorolv/jdbc-mcp-server#readme",
  "repository": {
    "url": "https://github.com/igorolv/jdbc-mcp-server",
    "source": "github"
  },
  "packages": [
    {
      "registryType": "oci",
      "identifier": "ghcr.io/igorolv/jdbc-mcp-server:0.0.0",
      "transport": {
        "type": "stdio"
      },
      "runtimeArguments": [
        {
          "type": "named",
          "name": "--mount",
          "value": "type=bind,src={data_dir},dst=/data",
          "description": "Host directory holding connections.json; the server also keeps its local catalogs and logs there.",
          "isRequired": true,
          "variables": {
            "data_dir": {
              "description": "Host directory containing connections.json (for example ~/.jdbc-mcp-server)",
              "format": "filepath",
              "isRequired": true
            }
          }
        }
      ],
      "environmentVariables": [
        {
          "name": "JDBC_MCP_RESOURCES_ENABLED",
          "description": "Also expose catalog-qualified MCP resources for table and column metadata",
          "default": "false",
          "isRequired": false
        }
      ]
    }
  ]
}
